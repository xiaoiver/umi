# umi-plugin-ssr

## Usage

Install via yarn or npm.

```bash
$ yarn add umi-plugin-ssr
```

Configure it in the `.umirc.js`.

```js
export default {
  plugins: [
    ['umi-plugin-ssr', option],
  ],
};
```

## Option
